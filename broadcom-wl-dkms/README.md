## broadcom-wl-dkms system extension

Simple script to add a system extension in /var/lib/extensions and to add the firmware files necessary for certain broadcom devices to make wifi work

The firmware files are taken from the [archlinux broadcom-wl-dkms package](https://archlinux.org/packages/extra/x86_64/broadcom-wl-dkms).
