## broadcom-bt-firmware system extension

Simple script to add a system extension in /var/lib/extensions and to add the firmware files necessary for certain broadcom devices to make bluetooth work

The firmware files are taken from [winterheart github repository](https://github.com/winterheart/broadcom-bt-firmware).
